$(document).ready(function() {
  $('.slider__content').slick({
    prevArrow: $('.prev'),
    nextArrow: $('.next'),
    touchMove: false
  });

  $('.preview-text').slick({
    prevArrow: $('.prev1'),
    nextArrow: $('.next1')
  });


});